# docs/specs/

Build specifications (work orders) authored by Orchestr8's Build Spec Council
(ADR 0003 · O1). Cursor executes them. After implementation, paste the diff back
for Challenge Council review (O2).

Each spec has:
- `<id>.md` — human + Cursor-facing work order
- `<id>.json` — machine-readable copy for O2 diff review
