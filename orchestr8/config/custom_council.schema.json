{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Orchestr8 Custom Council Save Request",
  "description": "Payload for POST /v1/councils — Save team in the AI team panel. Name and the selected roles are operator inputs; id, voting default, output owner and provenance are derived.",
  "type": "object",
  "required": ["name", "agents"],
  "properties": {
    "name": {
      "description": "Display label. Slugified into the council id.",
      "type": "string",
      "minLength": 2,
      "maxLength": 60
    },
    "purpose": {
      "description": "One-line summary shown on the council button.",
      "type": "string",
      "minLength": 0,
      "maxLength": 280
    },
    "agents": {
      "description": "Role ids in run order.",
      "type": "array",
      "minItems": 1,
      "maxItems": 24,
      "items": { "type": "string", "minLength": 1, "maxLength": 80 }
    },
    "mode": {
      "description": "Execution mode. single is stored as parallel with one agent.",
      "type": "string",
      "enum": ["pipeline", "parallel", "single"]
    },
    "voting": {
      "type": "string",
      "enum": ["none", "veto_on_critical", "dissent_required"]
    }
  }
}
