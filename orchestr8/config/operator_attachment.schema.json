{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Orchestr8 Operator Attachment",
  "description": "A text file the operator attached to a council run. Injected into COLLECTION CONTEXT, not stored as VIP truth.",
  "type": "object",
  "required": ["name", "text"],
  "properties": {
    "name": { "type": "string", "minLength": 1, "maxLength": 180 },
    "text": { "type": "string", "maxLength": 24000 },
    "source": { "type": "string", "enum": ["upload", "repo", "paste"] }
  }
}
