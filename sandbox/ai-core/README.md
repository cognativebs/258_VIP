# AI Core — WebGL Sandbox

Standalone Three.js recreation of the stacked neon AI-core visual.
Not part of the VIP product milestone.

## Run

```bash
cd sandbox/ai-core
npm install
npm run dev
```

Opens at `http://localhost:5180`.

## Controls

- Drag — orbit camera
- Scroll — zoom
- Hover a disc — intensify its glow
