.env
.env.*
*.env
secrets.json
node_modules/
dist/
.next/
out/
services/jobs/.state/
.state/
# Manual intelligence store (predictions/underwriting an operator typed in).
# Local state, not source; override the path with VIP_INTELLIGENCE_STATE.
scripts/dev-intelligence-state.json
*.tsbuildinfo
coverage/
.DS_Store
tmp_*.py
infra/db/migrations/_verify*.sql
__pycache__/
*.py[cod]
.pytest_cache/
scripts/logs/*.log
clz-inbox/**/*.xml
clz-inbox/archive/
clz-inbox/processed/
clz-inbox/.hashes.json

# Placeholder JPEGs written by Batch 001 until the Ricoh drop is live
data/scan-inbox/batch-001-sports/*.jpg

# Immutable scan masters (copied at ingest). Do not commit operator scans.
data/scan-masters/
data/scan-inbox/uploads/

# eBay marketplace-deletion Worker secrets (never commit)
infra/ebay-deletion-worker/.verification-token
infra/ebay-deletion-worker/.dev.vars
infra/ebay-deletion-worker/.live-url
infra/ebay-deletion-worker/.claim-url

# Test scratch files written by services/api tests
services/api/.tmp-test-signals-feed.json
services/api/sources-state-api-test.json

# Per-operator decision constraints (copy from user-constraints.example.json)
services/api/user-constraints.json

# Leftover Binder SQLite (import-only; live layout is Postgres vault_tcg)
*.sqlite
*.sqlite-wal
*.sqlite-shm
apps/binder-vault/.data/
